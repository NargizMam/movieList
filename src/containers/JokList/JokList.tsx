import React from 'react';

interface Props {
    show: boolean;
}

const JokList: React.FC<Props> = ({show}) => {
    return (
        <div>
            
        </div>
    );
};

export default JokList;